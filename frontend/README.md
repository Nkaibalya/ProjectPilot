# ProjectPilot - Frontend

This is the React frontend for the ProjectPilot application.

### Setup
1. `npm install`
2. Configure `.env` with `REACT_APP_API_URL`
3. `npm start`

For full project documentation and setup instructions (including backend and demo data seeding), please refer to the main [README.md](../README.md) in the root directory.
